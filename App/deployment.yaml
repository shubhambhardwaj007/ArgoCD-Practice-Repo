apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache-webserver-argocd
spec:
  selector:
    matchLabels:
      app: mywebserver
  replicas: 2
  template:
    metadata:
      labels:
        app: mywebserver
    spec:
      containers:
      - name: webserver-container
        image: asia.gcr.io/searce-playground-v1/shubham-versioning:a17c779 
        ports:
        - containerPort: 80
